name: canary
root: /Users/stoscanini/dev/work/canary/backend/canary

windows:
  - canary-app:
      layout: main-vertical
      panes:
        - make up
  - ngrok:
      layout: main-vertical
      panes:
        - ngrok http --url=unique-hermit-centrally.ngrok-free.app 8000
  - claude-code:
      layout: main-vertical
      panes:
        - claude
  - shell:
     layout: main-vertical
